---
title: "App Dashboard"
description: "Understand your app's central hub and how to track analysis progress."
icon: "gauge"
---

{/* screenshot:vibescale-app-dashboard */}
<Frame caption="VibeScale app dashboard — central hub with Opportunity and Security tracks">
  <img
    src="/images/screenshots/vibescale-app-dashboard.png"
    alt="VibeScale app dashboard — central hub with Opportunity and Security tracks"
  />
</Frame>


The dashboard is your app’s control center. From here, you start analyses, monitor progress in real time, and access the results of each stage.

## App Information

At the top of the dashboard, you’ll find the app’s name and description exactly as registered. These fields provide context for all generated reports.

## Analysis Tracks

VibeScale organizes the analysis into two independent tracks, each with its own stages and status indicators.

<CardGroup cols={2}>
  <Card title="Opportunity" icon="chart-line">
    **Discovery → Alignment → Report**

    Assesses market maturity, product-market fit, and generates a report with positioning recommendations.
  </Card>
  <Card title="Security" icon="shield-check">
    **Code → Deps → Secrets → Runtime**

    Scans source code, dependencies, exposed secrets, and the production attack surface.
  </Card>
</CardGroup>

## Status Indicators

Each stage in a track shows one of the following states:

| Status       | Meaning                                   |
|--------------|-------------------------------------------|
| Pending      | The stage has not been started yet.       |
| In Progress  | The analysis is currently running.         |
| Completed    | Results are available.                      |

## Action Buttons

- **Start Opportunity Analysis** — triggers the full Opportunity track (Discovery → Alignment → Report).
- **Start Security Check** — triggers the full Security track (Code → Deps → Secrets → Runtime).
- **Run Re-Check** — performs a new security scan after fixes.

## Gate System

<Note>
  The **Security** track is only unlocked after completing the Opportunity analysis. Similarly, the **Re-Check** becomes available only after all critical and high vulnerabilities identified in the previous scan are resolved. This sequencing ensures you act on the right information in the right order.
</Note>

## Alignment (Alignment Check)

Once the Discovery analysis is complete, VibeScale presents an assistant to validate with you the automatically inferred information, such as main pain points, target audience, differentiators, and competitors.

To ensure the data shown in the assistant reflects the analysis state at the time you start validation, the system creates an instant snapshot of the inferred data. This prevents automatic updates during your session from changing the information you are reviewing.

You can start the assistant by clicking the "Start Alignment Check" button when it becomes available.

After confirming the information, your data is saved with reference to the snapshot used, ensuring consistency between what you saw and what was recorded.
